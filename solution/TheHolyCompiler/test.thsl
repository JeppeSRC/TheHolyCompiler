//#define DANK "Bruh"

#ifdef DANK
#message "Dank Works"
#elif defined(DADDY)
#message "Daddy Works, defining DANK to Bruh"
#define DANK "Bruh"
#else
#error "Y'all suck"
#endif

layout (location = 0) in vec3 position;

layout (binding = 0, set = 0) uniform Buffer0 {
    mat4 MVP;
};

out vec4 pos = THSL_Position;

float32 dot(vec3 v1, vec3 v2);

struct O {
    uint32 someOtherName;
};

struct Dank {
    uint32 fist;
    O o;
};

void main() {

    Dank d;

    uint32 a = d.o.someOtherName;

    vec3 v1;
    vec3 v1;

    float res = dot(v1, v2);

    pos = MVP * vec4(position, 1);

    float a = 123.123;

    return 0;
}

float dot(vec3 v1, vec3 v2) {

    
}
