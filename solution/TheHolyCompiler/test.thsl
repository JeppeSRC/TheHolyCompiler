#ifdef DANK
#message "Dank Works"
#elif defined(DADDY)
#message "Daddy Works, defining DANK to Bruh"
#define DANK "Bruh"
#define CONSTANT 1.0
#else
#error "Y'all suck"
#endif

layout (location = 0) in vec3 position;

layout (binding = 0, set = 0) uniform Buffer0 {
    mat4 MVP;
};

out vec4 pos = THSL_Position;

float dot(vec3 v1, vec3 v2);

struct O {
    uint32[10] someOtherName;
    vec2 shit;
};

struct Dank {
    O[2] o;
};

void main() {
    bool tmp = false;

    vec3 v1;
    vec3 v2 = vec3(CONSTANT, 0.0, CONSTANT);

    float res = dot(v1, v2);
    Dank d;

    vec2 a = d.o[0].shit.xy;

    if (tmp) a.x = d.o[0].someOtherName[5];
    else {
        a.y = 1.23;
    }

    pos = Buffer0.MVP * vec4(position, 1.0);

    return;
}

float dot(const vec3 v1, vec3& v2) {
    return v1.x * v2.x + v1.y * v2.y + v1.z * v2.z;    
}
