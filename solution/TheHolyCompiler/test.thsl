#ifdef DANK
#message "Dank Works"
#elif defined(DADDY)
#message "Daddy Works, defining DANK to Bruh"
#define DANK "Bruh"
#define CONSTANT 1.0
#else
#error "Y'all suck"
#endif

layout (location = 0) in vec3 position;

layout (binding = 0, set = 0) uniform Buffer0 {
    mat4 MVP;
};

out vec4 pos = THSL_Position;

float dot(vec3 v1, vec3 v2);

struct O {
    uint32[10] someOtherName;
    vec4 shit;
};

struct Dank {
    O[2] o;
};

void main() {
    vec3 v1 = vec3(0.0, 0.0, 0.0);
    vec3 v2 = vec3(0.0, 0.0, 0.0);

    v1.zy = (v1.xxx + v2.yyy).zz * dot(v1, v2);

    pos = Buffer0.MVP * vec4(position, 1.0);
}

float dot(vec3 v1, vec3 v2) {
    v1 *= v2;
    return v1.x + v1.y + v1.z;
}
